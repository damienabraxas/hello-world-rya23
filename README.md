# my-app

A minimal Python Hello World project.

**Ticket:** RYA-23 — initial scaffold with a runnable `hello.py` script.

## Requirements

- Python 3.8+

## Usage

Run the script from the project root:

```bash
python hello.py
```

Expected output:

```
Hello, World!
```

## Project layout

```
my-app/
├── hello.py    # Hello World entry point
└── README.md   # This file
```

## Development

No third-party dependencies are required. Clone or open this directory, then run `python hello.py` to verify the setup.
